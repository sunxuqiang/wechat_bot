你是一个专业的文档分块专家。请将以下文本进行智能分块，确保语义完整性和单词完整性。

分块要求：
1. 基本原则：
   - 保持章节结构：遵循文档的自然分隔点（如章节标题、段落等）
   - 保持单词完整性：绝不能在单词中间切分
   - 保持句子完整性：优先在句子边界处分块
   - 保持段落完整性：尽量不打断段落的语义连贯性
   
2. 分块优先级（从高到低）：
   - 章节标题（如"第X章"、"结语"、阿拉伯数字等）
   - 段落边界（连续换行）
   - 句子边界（句号、问号、感叹号等）
   - 从句边界（分号、冒号等）
   - 词组边界（逗号等）

3. 语言特定规则：
   - 英文：在空格、标点符号处分割，绝不能切断单词
   - 中文：在句号、问号、感叹号等处分割，保持语义完整
   - 混合文本：同时遵循中英文规则，确保双语文本的完整性
   - 不要改变原文的语言，即原文是英文就返回英文，原文为中文就返回中文

4. 块的大小控制：
   - 每个块应包含完整的语义单元
   - 块的大小应相对均衡，但不强制完全相等
   - 宁可块稍大，也不破坏语义完整性

5. 特殊处理：
   - 列表项：保持列表项的完整性
   - 表格：作为完整单元处理
   - 代码块：保持代码块的完整性
   - 引用：保持引用内容的完整性

请严格按照以下JSON格式返回，不要添加任何其他内容：

{{
    "chunks": [
        {{
            "content": "完整的文本块内容（确保单词和句子完整性）",
            "type": "块类型（章节/段落/列表等）",
            "summary": "该块的简要描述"
        }}
    ]
}}

待分块文本：
{text}

只返回严格的JSON，不要输出任何解释、注释或多余内容，JSON必须能被Python json.loads()直接解析。 